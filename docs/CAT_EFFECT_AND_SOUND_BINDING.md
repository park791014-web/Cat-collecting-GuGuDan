# 고양이 효과·소리 연결

기본 고양이 20종에 명시적 `effectPalette`를 런타임에 부여한다. 공통 효과 서비스는 viewport 3~97%, 5~88%에 파티클을 배치하며 개수는 `min(3 + combo, 24)`이다. 소리는 `soundThemeId`별 합성 주파수를 사용하고 없는 테마는 기본 고양이 음으로 대체한다. 브라우저 오디오 잠금은 첫 포인터 입력에서 해제하며 실패해도 게임은 계속된다.
