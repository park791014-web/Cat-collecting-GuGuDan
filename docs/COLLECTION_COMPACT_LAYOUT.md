# 도감 소형 썸네일 배치

- 기본 보기: `compact`, 기본 정렬: `rarity`.
- 전설 → 영웅 → 희귀 → 일반 순서이며 같은 등급은 기존 `displayOrder`를 유지한다.
- 모바일은 한 열, 720px 이상은 두 열이다.
- 첫 화면 이미지 크기는 52~72px이며 설명은 상세 화면에서만 표시한다.
- 중복 별은 0.82em, 상세 화면은 0.95em이다.
- 카드와 자식 요소에 `min-width:0`과 `box-sizing:border-box`를 적용해 가로 스크롤을 방지한다.
