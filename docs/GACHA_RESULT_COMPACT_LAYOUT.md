# 뽑기 결과 압축 레이아웃

최종 순서는 이미지 → 등급 → 이름 → 전체 설명 → 신규/중복 정보 → 확인 버튼이다. 결과용 중복 별은 DOM에서 제거한다. 신규 안내는 한 줄로, 중복은 획득 안내와 `등급 조각 +N · 현재 N개`를 같은 작은 영역에 배치한다. 설명은 `word-break:keep-all`, `overflow-wrap:break-word`, 균형 줄바꿈을 사용하며 `descriptionLines`가 있으면 명시된 줄을 우선한다.
