# 고양이 집 뽑기 연출

결과와 재화는 기존 서비스가 먼저 저장하고, 결과를 `pendingDrawReveal`로 보존한 뒤 고양이 집 연출을 시작한다. 상태는 `idle → animating → revealed → closing → idle`이다. 스킵은 같은 확정 결과를 즉시 공개하며 재계산·재차감하지 않는다. 새로고침 시 pending 결과를 바로 공개하고 확인 시 제거한다. 동작 줄이기 설정에서는 연출 시간이 단축된다.
