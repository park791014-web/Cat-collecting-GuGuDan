# 고양이 연출 스킬

영웅·전설은 `presentationSkill.effectThemeId`와 `soundThemeId`를 가진다. 전설은 점수에 영향을 주지 않는 `legendarySkill.specialOption` 하나를 추가한다. 기존 성능형 `skill` ID는 호환을 위해 유지하지만 `skillActive=false`로 실행을 막아 공식 타임어택과 모험 수치에 영향을 주지 않는다.
