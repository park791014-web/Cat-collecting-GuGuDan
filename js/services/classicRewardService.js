(function(global){'use strict';var v2=global.GugudanV2;function calculate(result,isBest){var c=v2.classicRewardConfig,correct=Math.max(0,Math.floor(Number(result.correctCount)||0)),perfect=Number(result.accuracy)===100;var parts={completion:c.completionCoins,correct:correct*c.coinsPerCorrectAnswer,perfect:perfect?c.perfectAccuracyBonus:0,personalBest:isBest?c.personalBestBonus:0};parts.total=parts.completion+parts.correct+parts.perfect+parts.personalBest;return parts;}function claim(result,isBest){if(!result||!result.sessionId)return{ok:false,reason:'missing_session'};var save=v2.storageService.loadSaveData(),history=save.rewardHistory.classicRewardedSessionIds||(save.rewardHistory.classicRewardedSessionIds=[]);if(history.indexOf(result.sessionId)>=0)return{ok:false,reason:'already_rewarded',parts:calculate(result,isBest),coins:save.currency.coins};var parts=calculate(result,isBest);save.currency.coins=Math.max(0,(Number(save.currency.coins)||0)+parts.total);history.push(result.sessionId);save.rewardHistory.classicRewardedSessionIds=history.slice(-v2.classicRewardConfig.rewardedSessionLimit);if(!v2.storageService.saveSaveData(save))return{ok:false,reason:'save_failed'};return{ok:true,parts:parts,coins:save.currency.coins};}v2.classicRewardService={calculate:calculate,claim:claim};})(window);
