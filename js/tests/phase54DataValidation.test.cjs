const fs=require('fs'),vm=require('vm'),assert=require('assert');global.window=global;global.GugudanV2={};['js/config/catDrawConfig.js','js/config/cardPackConfig.js','js/config/dailyMissionConfig.js','js/data/cats.js','js/data/seasonCats.js'].forEach(file=>vm.runInThisContext(fs.readFileSync(file,'utf8'),{filename:file}));
const v=GugudanV2,keys=['normal','rare','hero','legendary'],expected={coin:[.60,.30,.09,.01],normal:[.60,.30,.09,.01],premium:[.45,.35,.17,.03],season:[.45,.35,.17,.03]},actual={coin:v.catDrawConfig.rarityRates,normal:v.cardPackConfig.normalPack.rarityRates,premium:v.cardPackConfig.premiumPack.rarityRates,season:v.cardPackConfig.seasonPack.rarityRates};
Object.keys(actual).forEach(type=>{assert(Math.abs(keys.reduce((a,k)=>a+actual[type][k],0)-1)<1e-9);assert.deepEqual(keys.map(k=>actual[type][k]),expected[type]);});
const cats=[...v.baseCats,...v.seasonCats];assert(cats.every(c=>c.description));assert(cats.every(c=>!/영웅|전설|히어로|레전더리/.test(c.displayName)));assert(cats.filter(c=>c.rarity==='hero'||c.rarity==='legendary').every(c=>c.presentationSkill&&c.skillActive===false));assert(cats.filter(c=>c.rarity==='legendary').every(c=>c.legendarySkill&&!c.legendarySkill.affectsScore));
assert.equal(Object.values(v.dailyMissionConfig).filter(m=>(m.reward.premiumTickets||0)>0).length,1);
function simulate(rates,count=10000){const out={normal:0,rare:0,hero:0,legendary:0};for(let i=0;i<count;i++){let n=Math.random(),sum=0;for(const k of keys){sum+=rates[k];if(n<sum){out[k]++;break;}}}return Object.fromEntries(keys.map(k=>[k,Math.round(out[k]/count*10000)/100]));}
const distributions=Object.fromEntries(Object.entries(actual).map(([k,r])=>[k,simulate(r)]));console.log(JSON.stringify({passed:true,drawRates:actual,simulated10000:distributions,catCount:cats.length,premiumDailyMissionCount:1},null,2));
