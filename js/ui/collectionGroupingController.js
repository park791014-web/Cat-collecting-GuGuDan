(function(global){'use strict';var labels={normal:'일반 고양이',rare:'희귀 고양이',hero:'영웅 고양이',legendary:'전설 고양이'},order=['normal','rare','hero','legendary'],timer=null,busy=false;function group(){var grid=document.getElementById('collection-grid');if(!grid||busy||grid.querySelector('.collection-rarity-row'))return;var cards=Array.prototype.slice.call(grid.children).filter(function(node){return node.classList&&node.classList.contains('cat-thumbnail-item');});if(!cards.length)return;busy=true;order.forEach(function(rarity){var matching=cards.filter(function(card){return card.classList.contains('rarity-'+rarity);});if(!matching.length)return;var section=document.createElement('section'),title=document.createElement('h3'),track=document.createElement('div');section.className='collection-rarity-row rarity-row-'+rarity;title.className='collection-rarity-row__title';title.textContent=labels[rarity]+' · '+matching.length+'종';track.className='collection-rarity-row__track';matching.forEach(function(card){track.appendChild(card);});section.appendChild(title);section.appendChild(track);grid.appendChild(section);});busy=false;}function schedule(){clearTimeout(timer);timer=setTimeout(group,0);}function init(){var grid=document.getElementById('collection-grid');if(!grid)return;new MutationObserver(schedule).observe(grid,{childList:true});schedule();}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();global.groupCollectionByRarity=group;})(window);
